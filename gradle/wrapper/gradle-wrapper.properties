distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-8.14.3-bin.zip
# TODO: pin the distribution's checksum so a tampered download is rejected.
# The sandbox this was generated in cannot reach services.gradle.org, so the
# value could not be filled in here. On a machine with network access run:
#   curl -sSL https://services.gradle.org/distributions/gradle-8.14.3-bin.zip.sha256
# and paste the result as:
#   distributionSha256Sum=<64 hex chars>
networkTimeout=10000
validateDistributionUrl=true
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
